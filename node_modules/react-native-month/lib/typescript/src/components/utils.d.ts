import { MonthProps, DayType } from '../types';
export declare function getMonthDays(month: number, year: number, firstDayMonday: boolean, disableRange: boolean, disabledDays: {
    [key: string]: any;
}, disableOffsetDays: boolean, startDate?: Date, endDate?: Date, minDate?: Date, maxDate?: Date, showSixWeeks?: boolean): DayType[];
export declare function areEqual(prevProps: MonthProps, nextProps: MonthProps): boolean;
