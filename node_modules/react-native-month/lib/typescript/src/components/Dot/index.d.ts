/// <reference types="react" />
import { DayDot, DotTheme } from '../../types';
interface Props extends DayDot, DotTheme {
    active: boolean;
    index: number;
}
declare function Dot({ active, color, index, selectedColor, dotContainerStyle, }: Props): JSX.Element;
export default Dot;
