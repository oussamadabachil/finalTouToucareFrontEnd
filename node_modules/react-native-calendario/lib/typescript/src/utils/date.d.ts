import { LocaleType } from '../types';
export declare function addDays(date: Date, days: number): Date;
export declare function isValidDate(date: Date): boolean;
export declare function isSameDate(one: Date, other: Date): boolean;
export declare function getMonthIndex(firstMonth: Date, date: Date, months: any[], numberOfMonths: number): number | null;
export declare function getMonthNames(locale?: LocaleType): string[];
