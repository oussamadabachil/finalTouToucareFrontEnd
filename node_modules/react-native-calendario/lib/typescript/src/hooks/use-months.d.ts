import { CalendarProps } from '../types';
interface Input extends Pick<CalendarProps, 'startingMonth' | 'numberOfMonths'> {
}
interface Result {
    firstMonth: Date;
    months: any[];
}
export default function useMonths({ startingMonth, numberOfMonths, }: Input): Result;
export {};
