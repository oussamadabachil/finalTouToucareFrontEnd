import { CalendarProps } from '../types';
interface Input extends Pick<CalendarProps, 'startDate' | 'endDate' | 'numberOfMonths'> {
    firstMonthToRender: Date;
}
interface Result {
    start?: Date;
    end?: Date;
}
export default function useRange({ startDate, endDate, numberOfMonths, firstMonthToRender, }: Input): Result;
export {};
